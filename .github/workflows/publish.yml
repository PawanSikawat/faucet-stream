name: Publish to crates.io

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run (cargo publish --dry-run)"
        required: false
        default: "false"
        type: boolean

env:
  CARGO_TERM_COLOR: always

jobs:
  publish:
    name: Publish
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.94.0"
          components: rustfmt, clippy

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Check formatting
        run: cargo fmt --all --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

      - name: Test
        run: cargo test --workspace --all-features

      # Publish in dependency order: core -> source/sink -> umbrella.
      # Sleep between publishes to allow crates.io index to propagate.
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y cmake libcurl4-openssl-dev pkg-config libssl-dev

      - name: Publish (dry run)
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.dry_run == 'true' }}
        run: |
          cargo publish --dry-run -p faucet-core
          cargo publish --dry-run -p faucet-source-rest
          cargo publish --dry-run -p faucet-source-graphql
          cargo publish --dry-run -p faucet-source-xml
          cargo publish --dry-run -p faucet-source-grpc
          cargo publish --dry-run -p faucet-source-postgres
          cargo publish --dry-run -p faucet-source-mysql
          cargo publish --dry-run -p faucet-source-kafka
          cargo publish --dry-run -p faucet-source-s3
          cargo publish --dry-run -p faucet-source-mongodb
          cargo publish --dry-run -p faucet-source-redis
          cargo publish --dry-run -p faucet-source-webhook
          cargo publish --dry-run -p faucet-source-csv
          cargo publish --dry-run -p faucet-source-elasticsearch
          cargo publish --dry-run -p faucet-sink-bigquery
          cargo publish --dry-run -p faucet-sink-postgres
          cargo publish --dry-run -p faucet-sink-jsonl
          cargo publish --dry-run -p faucet-sink-snowflake
          cargo publish --dry-run -p faucet-sink-mysql
          cargo publish --dry-run -p faucet-sink-sqlite
          cargo publish --dry-run -p faucet-sink-kafka
          cargo publish --dry-run -p faucet-sink-s3
          cargo publish --dry-run -p faucet-sink-mongodb
          cargo publish --dry-run -p faucet-sink-redis
          cargo publish --dry-run -p faucet-sink-csv
          cargo publish --dry-run -p faucet-sink-elasticsearch
          cargo publish --dry-run -p faucet-sink-http
          cargo publish --dry-run -p faucet-stream

      - name: Publish
        if: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.dry_run != 'true') }}
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          cargo publish -p faucet-core
          sleep 30
          cargo publish -p faucet-source-rest
          cargo publish -p faucet-source-graphql
          cargo publish -p faucet-source-xml
          cargo publish -p faucet-source-grpc
          cargo publish -p faucet-source-postgres
          cargo publish -p faucet-source-mysql
          cargo publish -p faucet-source-kafka
          cargo publish -p faucet-source-s3
          cargo publish -p faucet-source-mongodb
          cargo publish -p faucet-source-redis
          cargo publish -p faucet-source-webhook
          cargo publish -p faucet-source-csv
          cargo publish -p faucet-source-elasticsearch
          cargo publish -p faucet-sink-bigquery
          cargo publish -p faucet-sink-postgres
          cargo publish -p faucet-sink-jsonl
          cargo publish -p faucet-sink-snowflake
          cargo publish -p faucet-sink-mysql
          cargo publish -p faucet-sink-sqlite
          cargo publish -p faucet-sink-kafka
          cargo publish -p faucet-sink-s3
          cargo publish -p faucet-sink-mongodb
          cargo publish -p faucet-sink-redis
          cargo publish -p faucet-sink-csv
          cargo publish -p faucet-sink-elasticsearch
          cargo publish -p faucet-sink-http
          sleep 30
          cargo publish -p faucet-stream
