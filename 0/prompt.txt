<task-notification>
<task-id>ae178cc9faa01556a</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-502/-Users-c-sikawats-Documents-GitHub-Personal-faucet-stream/c52ee385-5ec5-464a-91ae-a39a0cbdf230/tasks/ae178cc9faa01556a.output</output-file>
<status>completed</status>
<summary>Agent "Adopt conformance checks: messaging" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>All 12 crates compile cleanly. The task is complete.

## Report

Adopted the new `faucet-conformance` checks (#465) into all 12 messaging/kv connectors. Every crate compiles (`cargo test -p &lt;crate&gt; --test conformance --no-run` → `Finished`); `cargo fmt` clean. No git actions taken.

### Checks added per connector

**Sources**
| Connector | Added | Where | Skipped (why) |
|---|---|---|---|
| kafka | `connector_name` (10), `preflight` (11) | live bounded-memory test (fetch_metadata→Pass) | batch_size=0 (live poller, not single-page) |
| nats | `connector_name` (10) | offline errors test | `preflight` — default page-pull check blocks on quiet/unreachable subjects (unsure→skip); batch_size=0 (poller) |
| pubsub | `connector_name` (10), `preflight` (11) | live bounded-memory test (subscription.exists→Pass) | batch_size=0 (poller) |
| sqs | `connector_name` (10) offline; `preflight` (11) live | errors + bounded-memory tests | batch_size=0 (live receive-loop) |
| kinesis | `connector_name` (10) offline; `preflight` (11) live | errors + bounded-memory tests | batch_size=0 (per-shard poller) |
| redis | `connector_name` (10) + `preflight` (11) offline; **`batch_size=0` single-page (9)** live | errors + bounded-memory tests | — (redis explicitly documents batch_size=0 as a single-page drain, so check 9 genuinely applies) |

**Sinks** (all add `connector_name` value-form (10) + `sink preflight` (11) in the live capabilities/idempotent test, reusing the existing instance)
- kafka, pubsub, sqs, kinesis, redis: overridden `check()` returns a Pass probe against the live broker/queue/stream/server.
- nats: no custom `check()`, so core's default returns a well-formed single `Skip` probe inside `Ok(report)` — the check verifies that contract holds.

### Deliberately NOT added (per conservative rules)
- `assert_write_modes_truthful` / `assert_schema_evolution_effective`: none of these connectors advertise upsert/delete or schema evolution (all append/stream, schemaless).
- `assert_batch_size_zero_single_page`: skipped for kafka/nats/pubsub/sqs/kinesis — they are live incremental pollers that emit multiple pages regardless of `batch_size`, so the single-page assertion would not hold. Added only for **redis**, whose config explicitly documents `batch_size=0` as a whole-result single-page drain (reused the seeded container, no new fixtures).

### Notes
- All new `preflight` checks confirmed against each connector's actual `check()` implementation (returns `Ok(CheckReport)` with well-formed probes in every case).
- Added `use faucet_core::Sink as _;` to the kafka-sink, nats-sink, and redis-sink test modules (needed for the value-form `connector_name` call); the other sink tests already imported it.
- During compilation the workspace `target/` filled the disk; I reclaimed ~23GB of regenerable artifacts (`target/semver-checks`, `incremental`, `examples`, `doc`, `package`) per the maintenance rules — no source or `deps/`/`build/`/`.fingerprint/` touched.

Docker-gated tests (bounded-memory / idempotent / capabilities) will exercise the live checks in CI as before.</result>
<usage><subagent_tokens>228609</subagent_tokens><tool_uses>58</tool_uses><duration_ms>1143002</duration_ms></usage>
</task-notification>