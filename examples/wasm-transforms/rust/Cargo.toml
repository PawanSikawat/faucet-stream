[package]
name = "faucet-wasm-example-add-field"
version = "0.0.0"
edition = "2021"
publish = false
description = "Reference faucet-stream WASM transform (Rust): adds a field and uppercases `name`."

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1"

[profile.release]
opt-level = "s"
lto = true
strip = true
